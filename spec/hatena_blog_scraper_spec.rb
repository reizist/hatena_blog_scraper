require "spec_helper"

describe HatenaBlogScraper do
  it "has a version number" do
    expect(HatenaBlogScraper::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
